export class User {
  constructor(public id: string, public name: string, public description: string, public web_service_url: string,
              public web_service_type: string, public image_address: string, public icon_address: string) {
  }
}
